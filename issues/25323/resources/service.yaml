apiVersion: v1
kind: Service
metadata:
  name: my-app-service
  labels:
    app: my-app
    test: test
    test2: test2
    test3: test3
  annotations:
    argocd.argoproj.io/sync-wave: "3"
spec:
  type: ClusterIP
  selector:
    app: my-app
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 80