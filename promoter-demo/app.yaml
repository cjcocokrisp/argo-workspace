apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: promoter-demo
  namespace: openshift-gitops
spec:
  project: default
  destination:
    server: https://kubernetes.default.svc
    namespace: openshift-gitops
  sourceHydrator:
    drySource:
      repoURL: https://github.com/cjcocokrisp/argo-workspace.git
      path: promoter-demo/manifests/chart
      targetRevision: HEAD
      helm:
        parameters:
          - name: replicas
            value: "3"
          - name: keyvalue
            value: "hello everyone!"
    hydrateTo:
      targetBranch: environments/openshift-test-next
    syncSource:
      targetBranch: environments/openshift-test
      path: hydrated
