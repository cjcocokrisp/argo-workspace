apiVersion: v1
kind: Service
metadata:
  name: service
  namespace: default
spec:
  type: NodePort
  selector:
    app: nginx
  ports:
    - port: 80
      targetPort: 80
      nodePort: {{ .Values.nodeport }}

